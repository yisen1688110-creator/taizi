# Placeholder for future DB (e.g., Postgres/MySQL). For now we use SQLite in backend volume.
FROM alpine:3.20
CMD ["/bin/sh","-c","echo SQLite is embedded in backend; this container is a placeholder && sleep infinity"]